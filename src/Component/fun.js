export function upper() {
    const inputStr = prompt("Enter a string to convert to uppercase:");
    return inputStr.toUpperCase();
  }
  
  export function lower() {
    const inputStr = prompt("Enter a string to convert to uppercase:");
    return inputStr.toLowerCase();
  }